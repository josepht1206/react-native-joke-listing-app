import React from "react";
import { Text, View, StyleSheet } from "react-native";

const CreateScreen = ({ navigation }) => {
  return (
    <View>
      <Text>Create Joke</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default CreateScreen;
